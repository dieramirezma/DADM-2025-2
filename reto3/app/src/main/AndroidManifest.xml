<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para el juego online -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_custom_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_custom_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reto3"
        tools:targetApi="31">

        <!-- Lista de juegos online - ACTIVIDAD PRINCIPAL -->
        <activity
            android:name=".GameListActivity"
            android:exported="true"
            android:label="@string/available_games"
            android:theme="@style/Theme.Reto3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad del juego online -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reto3" />

    </application>

</manifest>